# Entry receipt account fields

`Cash left uninvested` is the simulated cash not used to buy whole shares at the fill.

`Account equity at entry` is the full simulated account value at the moment of entry. It includes both the simulated position and uninvested simulated cash.

`Max planned loss` is the approximate dollar loss if the initial stop is reached from the simulated fill, based on the full initial share quantity.
